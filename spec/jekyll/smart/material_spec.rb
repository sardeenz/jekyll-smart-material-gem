require 'spec_helper'

describe Jekyll::Smart::Material do
  it 'has a version number' do
    expect(Jekyll::Smart::Material::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
